[workspace]
members = [
    "abi",
    "common",
    "builder",
    "cli",
    "daemon",
]

[profile.release]
opt-level = 's'
debug = true
rpath = false
debug-assertions = false
overflow-checks = false
